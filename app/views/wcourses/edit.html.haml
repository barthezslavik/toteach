= form_for @wcourse, :url => wcourse_path(@wcourse), :html => { :class => "edit_wcourse", :id => "edit_wcourse" } do |f|
  %input{:name => "_method", :type => "hidden", :value => "put"}/
  = render :partial => "form", :locals => {:f => f, :image => @wcourse.imgsrc}
